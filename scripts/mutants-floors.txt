# Mutation-testing ratchet floors. One `<crate> <percent>` pair per
# line. A nightly run fails if a crate's caught-mutant percentage
# drops more than 2.0 points below its recorded value
# here. `percent` is caught / (caught + missed); timeouts and unviable
# mutants are excluded from both sides. Regenerate locally with:
#   python3 scripts/mutants_gate.py --dir <mutants.out> --crate <crate> --update-floors
# CI never updates this file.
#
# Seeded 2026-09-08 (LAB-267). `openszigno-core` is a full-crate run
# (559 mutants: 433 caught, 51 missed, 64 unviable, 11 timeouts). Host time
# budget did not allow a full `openszigno-verify` run locally (1601 mutants,
# ~45 minutes extrapolated from timing); this floor is measured from an
# exact quarter of the crate's mutants (`--shard 1/4`, 401 mutants: 252
# caught, 105 missed, 44 unviable). The nightly workflow runs each crate in
# full and will move this floor to the true value on its first `--update-floors`
# run; treat this line as provisional until then.
openszigno-core 89.46
openszigno-verify 70.59
