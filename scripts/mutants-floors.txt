# Mutation-testing ratchet floors. One `<crate> <percent>` pair per
# line. A nightly run fails if a crate's caught-mutant percentage
# drops more than 2.0 points below its recorded value
# here. `percent` is caught / (caught + missed); timeouts and unviable
# mutants are excluded from both sides. Regenerate locally with:
#   python3 scripts/mutants_gate.py --dir <mutants.out> --crate <crate> --update-floors
# CI never updates this file.
#
# Updated 2026-09-08 (LAB-274, behavioural tests for the first batch of
# mutation-testing survivors). `openszigno-core` is a full-crate run (559
# mutants: 441 caught, 43 missed, 64 unviable, 11 timeouts). `openszigno-verify`
# is the same `--shard 1/4` slice the floor was originally seeded from (LAB-267:
# 401 mutants, 44 unviable, both then and now), so the change from 70.59% to
# 78.99% is a direct, apples-to-apples measurement of this change, not a
# different sample; the full-crate `openszigno-verify` measurement is still
# owed to the nightly workflow.
openszigno-core 91.12
openszigno-verify 78.99
